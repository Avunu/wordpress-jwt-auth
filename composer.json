{
    "name": "wordpress/jwt-auth",
    "description": "OIDC / proxy JWT authentication for WordPress, configured via wp-config.php",
    "type": "wordpress-plugin",
    "version": "1.0.1",
    "license": "MIT",
    "require": {
        "php": ">=8.4",
        "firebase/php-jwt": "*",
        "yahnis-elsts/plugin-update-checker": "^5.6"
    },
    "autoload": {
        "psr-4": {
            "JwtAuth\\": "src/"
        }
    },
    "config": {
        "optimize-autoloader": true,
        "sort-packages": true
    }
}
