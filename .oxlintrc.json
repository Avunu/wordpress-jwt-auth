{
	"$schema": "./node_modules/oxlint/configuration_schema.json",
	// All categories at error; "nursery" stays off (unstable rules). Every "off" below carries its
	// justification. Mirrors worker/.oxlintrc.json, minus the React/JSX plugins — nothing here
	// renders components; this is a vanilla DOM script.
	//
	// `worker` is ignored because that package lints itself with its own config and its own
	// (Workers-typed, not DOM-typed) program.
	"categories": {
		"correctness": "error",
		"nursery": "off",
		"pedantic": "error",
		"perf": "error",
		"restriction": "error",
		"style": "error",
		"suspicious": "error"
	},
	"ignorePatterns": [
		"build",
		"node_modules",
		"tests/playground",
		"vendor",
		"worker",
		"**/*.map",
		"**/*.d.ts"
	],
	"overrides": [
		{
			// Build/test tooling is Node-side: node: imports are the point, and a config file must
			// be a default export.
			"files": ["rolldown.config.ts", "vitest.config.ts"],
			"rules": {
				"import/no-nodejs-modules": "off",
				"import/no-default-export": "off",
				"no-console": "off"
			}
		}
	],
	"plugins": ["import", "unicorn", "oxc", "typescript"],
	"rules": {
		// ─── Configured (enabled with options) ────────────────────────────────
		// `== null` is the deliberate null-or-undefined check; all else strict.
		"eqeqeq": ["error", "always", { "null": "ignore" }],
		"eslint/no-empty": ["error", { "allowEmptyCatch": true }],
		"eslint/no-empty-function": ["error", { "allow": ["arrowFunctions"] }],
		"eslint/no-plusplus": ["error", { "allowForLoopAfterthoughts": true }],
		"eslint/no-use-before-define": ["error", { "functions": false, "variables": false }],
		// `void promise` is the explicit fire-and-forget marker.
		"eslint/no-void": ["error", { "allowAsStatement": true }],
		"new-cap": ["error", { "capIsNew": false }],

		// ─── Off: platform/domain conflicts ───────────────────────────────────
		// null is what querySelector returns; treating it as exotic helps nobody here.
		"unicorn/no-null": "off",
		"no-underscore-dangle": "off",

		// ─── Off: redundant with the TypeScript compiler ──────────────────────
		// tsc strict owns undefined-identifier checking (lint has no type info).
		"eslint/no-undef": "off",
		// Inference-first; tsc strict validates every boundary.
		"typescript/explicit-function-return-type": "off",
		"typescript/explicit-module-boundary-types": "off",

		// ─── Off: TypeScript idiom ────────────────────────────────────────────
		"eslint/no-undefined": "off",
		// Consecutive `const`s are separate statements because they are separate ideas; merging
		// them into one comma-chained declaration reads worse and churns every diff that adds one.
		"one-var": "off",

		// ─── Off: false positives on multi-line `//` blocks ───────────────────
		// The rule treats each `//` line as its own comment, so every continuation line of a
		// wrapped block comment is flagged for starting lowercase — mid-sentence, as it should.
		"eslint/capitalized-comments": "off",

		// ─── Off: contradicts an enabled rule or the formatter ────────────────
		"eslint/no-eq-null": "off",
		"import/prefer-default-export": "off",
		"import/no-named-export": "off",
		"no-duplicate-imports": "off",
		"unicorn/number-literal-case": "off",

		// ─── Off: deliberate architecture ─────────────────────────────────────
		"no-await-in-loop": "off",
		"eslint/require-await": "off",
		"import/no-relative-parent-imports": "off",
		"no-console": "off",
		"unicorn/consistent-function-scoping": "off",
		"unicorn/prefer-global-this": "off",

		// ─── Off: style preferences that fight convention ─────────────────────
		"func-style": "off",
		"id-length": "off",
		"complexity": "off",
		"max-depth": "off",
		"max-lines": "off",
		"max-lines-per-function": "off",
		"max-params": "off",
		"max-statements": "off",
		// Timeouts and sizes read fine inline next to their use.
		"no-magic-numbers": "off",
		"no-continue": "off",
		"no-inline-comments": "off",
		"no-negated-condition": "off",
		"unicorn/no-negated-condition": "off",
		"no-nested-ternary": "off",
		"unicorn/no-nested-ternary": "off",
		"no-ternary": "off",
		"init-declarations": "off",
		"sort-keys": "off",
		"sort-imports": "off",
		"import/exports-last": "off",
		"import/group-exports": "off",
		"import/max-dependencies": "off",
		"unicorn/import-style": "off",
		"eslint/prefer-named-capture-group": "off",
		"eslint/require-unicode-regexp": "off",

		// ─── Off: ES-downlevel restrictions (irrelevant on modern targets) ─────
		"oxc/no-async-await": "off",
		"oxc/no-optional-chaining": "off",
		"oxc/no-rest-spread-properties": "off"
	}
}
